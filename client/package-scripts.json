{
  "name": "portfolio-frontend",
  "version": "1.0.0",
  "description": "Frontend for professional portfolio application",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:tunnel": "TUNNEL_MODE=true next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "lint:fix": "next lint --fix",
    "test": "jest --watch",
    "test:ci": "jest --ci --coverage --watchAll=false",
    "type-check": "tsc --noEmit"
  }
}
